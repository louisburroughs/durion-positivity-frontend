# Durion Positivity Frontend

Angular 22 single-page app for the Durion POS platform. It follows the workspace ADRs, uses Signals and lazy-loaded feature domains, and talks to the backend through the platform gateway.

## Quick Start

```bash
npm install
npm start
```

## Common Checks

```bash
npm run build
npm test
npm run lint
```

## Related Docs

- `AGENTS.md` — repo-specific requirements and commands
- `../durion/AGENTS.md` — cross-repo guidance and shared config
- `../durion/knowledge-catalog/` — domain and module overview
