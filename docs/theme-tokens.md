# Theme Token Inventory

The canonical token inventory now lives at `design/source/theme-tokens.md`.

Use that file as the design-system source of truth for:

- palette tokens
- semantic tokens
- runtime theme tokens
- extension guidance for new CSS custom properties

This `docs/` copy is intentionally a pointer so app-facing documentation can reference the source-owned design artifact without maintaining duplicate content.
