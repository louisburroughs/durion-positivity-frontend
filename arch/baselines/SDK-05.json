[
  {
    "key": "src/app/features/billing/services/billing-transport.service.ts :: '/v1/invoices/'",
    "reason": "permanent exception: resolveArtifactDownloadUrl() builds a public, token-only download URL for an unauthenticated recipient (e.g. an emailed receipt link); no SDK operation returns a URL for this (downloadInvoiceArtifact streams the PDF body itself, which requires a bearer token this link's recipient doesn't have). Built from the injected InvoiceConfiguration.basePath, not environment.apiBaseUrl (SDK-06 fixed). PRD-sdk-migration-completion.md §Approved Exceptions.",
    "tracking": "#350"
  },
  {
    "key": "src/app/shared/bulk-import/services/bulk-import.service.ts :: '/v1/bulk-jobs/'",
    "reason": "permanent exception: buildTusUploadEndpoint()/isTrustedUploadUrl() no longer read environment.apiBaseUrl or window.location.origin directly (SDK-06/SDK-11 fixed) but still concatenate a hardcoded /v1 suffix onto the injected BulkLoaderConfiguration.basePath. The SDK's createTusUpload op exists, but tus-js-client owns the resumable-upload protocol end to end (its own creation POST, Location parsing, chunked PATCHes) and needs a bare endpoint URL, not an Observable-returning SDK call. PRD-sdk-migration-completion.md §Approved Exceptions.",
    "tracking": "#350"
  },
  {
    "key": "src/app/features/shell/services/chat-api.service.ts :: '/mcp-server/v1/mcp/chat'",
    "reason": "permanent exception (gateway/MCP chat traffic, no @durion-sdk package covers mcp-server; see SDK-03). PRD-sdk-migration-completion.md §Approved Exceptions.",
    "tracking": "#350"
  },
  {
    "key": "src/app/features/shell/services/chat-api.service.ts :: '/mcp-server/v1/mcp/documents'",
    "reason": "permanent exception (gateway/MCP chat traffic, no @durion-sdk package covers mcp-server; see SDK-03). PRD-sdk-migration-completion.md §Approved Exceptions.",
    "tracking": "#350"
  },
  {
    "key": "src/app/features/workexec/services/workexec.service.ts :: '/v1/workorders/'",
    "reason": "getWorkorderInvoiceView/requestInvoiceFinalization only (finalizeWorkorder removed, durion-positivity-backend#2210 ruling, issue #350): neither operation has a published contract in @durion-sdk/workorder or @durion-sdk/invoice. Blocked on durion-positivity-backend#2232 (WorkorderResponse needs an invoiceId, or a by-workorder invoice lookup) before these can move onto pos-invoice's getInvoice/finalizeInvoice.",
    "tracking": "#350"
  }
]
