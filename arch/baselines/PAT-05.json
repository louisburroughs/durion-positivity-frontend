[
  {
    "key": "src/app/features/crm/pages/party-contacts/party-contacts.component.ts :: deactivate :: toISOString().slice(0, 10) reads UTC, not local, date parts",
    "reason": "toISOString().slice(0, 10) reads the UTC date, not the local one (ADR-0038 \u00a71) \u2014 needs the local-date-parts helper (date-only.util.ts) instead.",
    "tracking": "TBD"
  },
  {
    "key": "src/app/features/shopmgmt/pages/mechanic-availability/mechanic-availability-page.component.ts :: todayIso :: toISOString().slice(0, 10) reads UTC, not local, date parts",
    "reason": "toISOString().slice(0, 10) reads the UTC date, not the local one (ADR-0038 \u00a71) \u2014 needs the local-date-parts helper (date-only.util.ts) instead.",
    "tracking": "TBD"
  },
  {
    "key": "src/app/features/workexec/services/workexec.service.ts :: listTechniciansForLocation :: toISOString().slice(0, 10) reads UTC, not local, date parts",
    "reason": "toISOString().slice(0, 10) reads the UTC date, not the local one (ADR-0038 \u00a71) \u2014 needs the local-date-parts helper (date-only.util.ts) instead.",
    "tracking": "TBD"
  },
  {
    "key": "src/app/features/workexec/services/workexec.service.ts :: submitTravelSegments :: toISOString().slice(0, 10) reads UTC, not local, date parts",
    "reason": "toISOString().slice(0, 10) reads the UTC date, not the local one (ADR-0038 \u00a71) \u2014 needs the local-date-parts helper (date-only.util.ts) instead.",
    "tracking": "TBD"
  }
]
