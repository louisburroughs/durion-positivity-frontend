[
  {
    "key": "src/app/features/platform/pages/tenant-detail/tenant-detail-page.component.ts :: confirmTransition :: errorKey.set not immediately preceded by state.set('error')",
    "reason": "state.set(this.stateFor(outcome.kind)) computes the state through a helper method rather than an inline literal/ternary, so the AST check can't see it resolves to 'error'/'forbidden' — behaviourally correct, but obscures the ADR-0031 §5 ordering at the call site. Inline the ternary or extend the rule to trust a same-class stateFor()-shaped helper.",
    "tracking": "#342"
  },
  {
    "key": "src/app/features/platform/pages/tenant-detail/tenant-detail-page.component.ts :: load :: errorKey.set not immediately preceded by state.set('error')",
    "reason": "state.set(this.stateFor(outcome.kind)) computes the state through a helper method rather than an inline literal/ternary — same shape as confirmTransition above.",
    "tracking": "#342"
  },
  {
    "key": "src/app/features/positivity/components/supplier-fleet-authorization-panel/supplier-fleet-authorization-panel.component.ts :: constructor :: errorKey.set not immediately preceded by state.set('error')",
    "reason": "the 422 branch sets state.set('unreachable') immediately before errorKey.set(...) — a third, non-error/forbidden page state the rule doesn't recognise as compliant (only 'error' and the forbidden/error ternary are). The ordering is followed; the state name isn't one of the two the rule accepts.",
    "tracking": "#342"
  },
  {
    "key": "src/app/features/positivity/components/supplier-fleet-lookup-panel/supplier-fleet-lookup-panel.component.ts :: constructor :: errorKey.set not immediately preceded by state.set('error')",
    "reason": "same 'unreachable' third-state shape as supplier-fleet-authorization-panel above.",
    "tracking": "#342"
  },
  {
    "key": "src/app/features/shopmgmt/pages/schedule-view/schedule-view-page.component.ts :: load :: errorKey.set not immediately preceded by state.set('error')",
    "reason": "genuine ADR-0031 §5 ordering bug: the error callback calls this.errorKey.set('SHOPMGMT.SCHEDULE_VIEW.ERROR_LOAD') BEFORE this.state.set('error') (reversed from every other page in the codebase). Worth its own fix/GitHub issue.",
    "tracking": "#342"
  }
]
