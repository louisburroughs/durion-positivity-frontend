/** CON-03 self-test fixture sibling. Deliberately exercises only one of the two public methods. */
describe('FxconCon03Service', () => {
  it('covered', () => {
    expect(true).toBe(true);
  });
});
